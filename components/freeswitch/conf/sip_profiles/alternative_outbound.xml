<profile name="alternative-outbound">
  <settings>
    <X-PRE-PROCESS cmd="include" data="shared/settings.xml"/>

    <param name="sip-port" value="5081"/>
    <param name="ext-sip-ip" value="$${alternative_sip_outbound_ip}"/>
    <param name="ext-rtp-ip" value="$${alternative_rtp_ip}"/>
  </settings>
</profile>

